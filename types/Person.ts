export default interface Person {
    id: Number,
    firstName: string,
    lastName: string,
    email: string,
    imageUrl?: string,
    mobile: string,
    street: string,
    sexId: number,
    statusId: number
    campusId: number,
}