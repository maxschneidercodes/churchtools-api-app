export let cachedPersons: any = []
export let cachedSelectState = 1

export function setCachedPersons(value: any) {
    cachedPersons = value
}

export function setCachedSelectState(value: any) {
    cachedSelectState = value
}