{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "PORT=5100 nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.4.1"
  },
  "dependencies": {
    "@churchtools/churchtools-client": "^1.3.6",
    "bootstrap": "^5.2.3"
  }
}
